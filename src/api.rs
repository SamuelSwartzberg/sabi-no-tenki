pub mod metaweather_query;

pub trait Query{
 // fn build_request(&self, options: &ProgOptions );
 // fn parse_result(&self) -> Vec<WeatherItem>;
 // Ignore for now:  fn get_names(&self) -> Vec<&str>;
 // Ignore for now: fn transform_generic_metric_name_to_api_specific(&self, metrics: &Vec<MetricType>) -> Vec<String>;
}
